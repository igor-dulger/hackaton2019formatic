pragma solidity ^0.5.0;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../../contracts/libs/ListAsRingLib.sol";
import "../../contracts/libs/mocks/ListAsRingMock.sol";

contract TestListLib {

    /* using ListLib for ListLib.ListData;
    ListLib.ListData private list;

    function testGetNextFromEmptyList() public {
        Assert.equal(list.getNextId(0), 0, "It should get 0.");
    }

    function testGetPrevFromEmptyList() public {
        Assert.equal(list.getPrevId(0), 0, "It should get 0.");
    } */
}
